<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Calculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Calculator" parent="Base.Theme.Calculator" />
    <style name="BotonLimpiar" parent="Widget.Material3.Button.ElevatedButton">
        <item name="android:shape">rectangle</item>
        <item name="cornerRadius">25dp</item>
        <item name="minWidth">0dp</item>
        <item name="minHeight">0dp</item>
        <item name="android:layout_marginEnd">5dp</item>
        <item name="android:layout_marginTop">15dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/naranja</item>
    </style>
    <style name="BotonOperacion" parent="Widget.Material3.Button.ElevatedButton">
        <item name="android:shape">rectangle</item>
        <item name="cornerRadius">10dp</item>
        <item name="minWidth">0dp</item>
        <item name="minHeight">0dp</item>
        <item name="android:layout_marginEnd">5dp</item>
        <item name="android:layout_marginTop">15dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/azul</item>
    </style>
    <style name="BotonNumero" parent="Widget.Material3.Button.ElevatedButton">
        <item name="android:shape">rectangle</item>
        <item name="cornerRadius">10dp</item>
        <item name="minWidth">0dp</item>
        <item name="minHeight">0dp</item>
        <item name="android:layout_marginEnd">5dp</item>
        <item name="android:layout_marginTop">15dp</item>
<!--        <item name="android:textColor">@color/white</item>-->
<!--        <item name="backgroundTint">@color/azul</item>-->
    </style>
</resources>